
name: Deploy iOS to App Store

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Yarn
        run: npm install -g yarn
      - name: Install dependencies
        run: yarn install
      - name: Set up Ruby for CocoaPods
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
      - name: Install CocoaPods
        run: sudo gem install cocoapods
      - name: Install Fastlane
        run: sudo gem install fastlane -NV
      - name: Set up Apple credentials
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: |
          mkdir -p mobile/ios/fastlane
          echo "$APP_STORE_CONNECT_API_KEY" > mobile/ios/fastlane/AuthKey.p8
      - name: Deploy to App Store
        run: |
          cd mobile/ios
          fastlane deploy
          # Forçage rechargement workflow GitHub Actions iOS
          # Trigger rescan GitHub Actions 2026-02-26
